- name: Trigger & Poll WP All Import Until Complete (GitHub) 🔄
        env:
          WP_SITE: ${{ secrets.WP_SITE }}
          WP_IMPORT_KEY: S5lQUiQkUZ  # Use the hardcoded value confirmed in our chat
          WP_IMPORT_ID: 14          # Use the hardcoded value confirmed in our chat
        run: |
          set -euo pipefail
          
          # The single, combined command that performs trigger AND one batch of process
          IMPORT_COMMAND_URL="${WP_SITE}/wp-load.php?import_key=${WP_IMPORT_KEY}&import_id=${WP_IMPORT_ID}&action=trigger&action=process"
          
          MAX_TRIES=60      # Max attempts (10 minutes total, 10s sleep)
          SLEEP_SECONDS=10  # Sleep time between batches
          
          echo "🚀 Starting combined WP All Import: Trigger and Process..."
          
          for i in $(seq 1 $MAX_TRIES); do
            echo "Attempt $i/$MAX_TRIES: Processing batch..."
            
            # Use curl to execute the combined command and capture the output status code
            # WP All Import returns '1' if more records exist, '0' when finished.
            STATUS_CODE=$(curl -s "$IMPORT_COMMAND_URL")
            
            if [ "$STATUS_CODE" == "0" ]; then
              echo "✅ WP All Import completed successfully!"
              exit 0
            fi
            
            if [ "$STATUS_CODE" != "1" ]; then
              echo "🛑 Received unexpected status code: $STATUS_CODE. Aborting import. Check WP All Import status."
              exit 1
            fi

            echo "⏳ Batch processed. Records remaining. Sleeping ${SLEEP_SECONDS}s before next batch..."
            sleep $SLEEP_SECONDS
          done

          echo "❌ Timed out waiting for WP All Import to finish after $MAX_TRIES attempts (10 minutes)."
          exit 1
